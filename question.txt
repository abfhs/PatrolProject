지금 admin페이지의 로그인에 문제가 있어.
frontend\src\pages\admin\AdminLogin.tsx의 로직에 문제가 있어 보이거든?

// 어드민 권한 확인
  if (response.user.role !== 'ADMIN') {
    setError('관리자 권한이 필요합니다.');
    return;
  }
이 로직에서 response.user의 값을 확인했을 때 아래와 같아. role값이 없거든? 로직을 수정해야할거같은데 확인해봐.

{email: 'superuser@admin.com', nickname: 'superuser', id: 6}